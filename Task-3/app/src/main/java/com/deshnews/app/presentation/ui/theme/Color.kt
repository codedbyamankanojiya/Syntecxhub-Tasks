package com.deshnews.app.presentation.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Deep Dark Canvas ──────────────────────────────────────────────────────────
val DeepNavy        = Color(0xFF0A0E17)   // Primary background
val DarkSlate       = Color(0xFF111827)   // Slightly lighter background
val CardSurface     = Color(0xFF151C2C)   // Card and component background
val CardBorder      = Color(0xFF1F293D)   // Subtle card border / separator
val ElevatedSurface = Color(0xFF1A2236)   // Elevated cards, image placeholders

// ─── Broadcast Red ────────────────────────────────────────────────────────────
val BroadcastRed    = Color(0xFFDC2626)   // Primary accent: header, badges, chips
val ElectricCrimson = Color(0xFFE11D48)   // Hover / pressed state for red elements
val DarkRed         = Color(0xFF991B1B)   // Dark variant: gradient end, deep shadows

// ─── Studio Gold / Yellow ─────────────────────────────────────────────────────
val StudioGold      = Color(0xFFFACC15)   // BREAKING badge left segment, active dots
val BrightGold      = Color(0xFFFBBF24)   // Hover state for gold elements

// ─── Typography ───────────────────────────────────────────────────────────────
val PureWhite       = Color(0xFFFFFFFF)   // Primary text on dark backgrounds
val MutedText       = Color(0xFF94A3B8)   // Secondary body text, timestamps
val SubtleText      = Color(0xFF64748B)   // Tertiary text, source names, hints

// ─── Status & Utility ─────────────────────────────────────────────────────────
val LiveGreen       = Color(0xFF22C55E)   // LIVE indicator dot
val NotificationRed = Color(0xFFEF4444)   // Notification badge counter
val OverlayDark     = Color(0xCC000000)   // Full-coverage scrim (80% black)
val OverlayMid      = Color(0x99000000)   // Medium scrim (60% black)
val OverlayLight    = Color(0x55000000)   // Light scrim (33% black)
val Transparent     = Color(0x00000000)   // Fully transparent
